# Generated by sila2.code_generator; sila2.__version__: 0.14.0
# -----
# This class does not do anything useful at runtime. Its only purpose is to provide type annotations.
# Since sphinx does not support .pyi files (yet?), this is a .py file.
# -----

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:

    from typing import Iterable, Optional

    from screenstreamer_types import GetLatestFrame_Responses, ListWindows_Responses, StartCapture_Responses, StopCapture_Responses
    from sila2.client import ClientMetadataInstance, ClientUnobservableProperty


class ScreenStreamerClient:
    """

    Captures screenshots of a target application window on the host PC.
    Uses Win32 API (PrintWindow/BitBlt) to capture the window even when
    partially obscured. Returns JPEG frames on demand.

    """

    IsCapturing: ClientUnobservableProperty[bool]
    """
    Whether the capture thread is currently running.
    """

    def StartCapture(
        self, WindowTitle: str, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> StartCapture_Responses:
        """

        Starts capturing a window whose title contains the given substring.
        A background thread continuously captures frames.

        """
        ...

    def StopCapture(self, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None) -> StopCapture_Responses:
        """
        Stops the background capture thread.
        """
        ...

    def ListWindows(
        self, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> ListWindows_Responses:
        """
        Returns a list of visible window titles on the server machine.
        Use these titles with StartCapture to begin streaming.
        """
        ...

    def GetLatestFrame(
        self, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> GetLatestFrame_Responses:
        """

        Returns the most recently captured frame as JPEG bytes.

        """
        ...
