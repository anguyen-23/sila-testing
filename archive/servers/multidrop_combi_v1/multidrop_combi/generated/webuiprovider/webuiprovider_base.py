# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

if TYPE_CHECKING:

    from ...server import Server


class WebUIProviderBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Provides a self-contained HTML page for device-specific UI rendering.
        When this feature is present, clients may use the CustomUI property to
        display a tailored interface instead of the generic dynamic view.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_CustomUI(self, *, metadata: MetadataDict) -> str:
        """

            A self-contained HTML page (with inline CSS and JavaScript) that
            renders a device-specific user interface. Intended to be displayed
            in a sandboxed iframe. Uses window.parent.postMessage() to request
            command execution and property reads from the host application.


        :param metadata: The SiLA Client Metadata attached to the call
        :return:
            A self-contained HTML page (with inline CSS and JavaScript) that
            renders a device-specific user interface. Intended to be displayed
            in a sandboxed iframe. Uses window.parent.postMessage() to request
            command execution and property reads from the host application.

        """
