# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .messagingclient_types import (
    ConfirmMessage_Responses,
    ConnectToMessageServer_Responses,
    DisconnectFromMessageServer_Responses,
)

if TYPE_CHECKING:

    from ...server import Server


class MessagingClientBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Connects to a lab messaging server via WebSocket to receive operator
        instructions for this instrument. Messages can be confirmed (acknowledged)
        once the operator has completed the requested action.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_IsListening(self, *, metadata: MetadataDict) -> bool:
        """
        Whether the WebSocket connection is active and listening for messages.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Whether the WebSocket connection is active and listening for messages.
        """

    @abstractmethod
    def get_PendingMessages(self, *, metadata: MetadataDict) -> str:
        """

            JSON string containing an array of pending (unconfirmed) messages.
            Each message has id, title, body, and instrument_id fields.


        :param metadata: The SiLA Client Metadata attached to the call
        :return:
            JSON string containing an array of pending (unconfirmed) messages.
            Each message has id, title, body, and instrument_id fields.

        """

    @abstractmethod
    def ConnectToMessageServer(
        self, ServerUrl: str, InstrumentId: str, *, metadata: MetadataDict
    ) -> ConnectToMessageServer_Responses:
        """

            Opens a WebSocket connection to the lab messaging server and begins
            listening for messages targeting this instrument.



        :param ServerUrl: URL of the messaging server (e.g. "http://localhost:8000").

        :param InstrumentId: Instrument identifier to filter messages for (e.g. "ps-tws-1").

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def DisconnectFromMessageServer(self, *, metadata: MetadataDict) -> DisconnectFromMessageServer_Responses:
        """
        Closes the WebSocket connection to the messaging server.


        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def ConfirmMessage(self, MessageId: str, *, metadata: MetadataDict) -> ConfirmMessage_Responses:
        """

            Confirms (acknowledges) a pending message by its ID, notifying
            the server that the operator has completed the requested action.



        :param MessageId: The ID of the message to confirm.

        :param metadata: The SiLA Client Metadata attached to the call

        """
