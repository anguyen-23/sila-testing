# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .screenstreamer_types import (
    GetLatestFrame_Responses,
    ListWindows_Responses,
    StartCapture_Responses,
    StopCapture_Responses,
)

if TYPE_CHECKING:

    from ...server import Server


class ScreenStreamerBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Captures screenshots of a target application window on the host PC.
        Uses Win32 API (PrintWindow/BitBlt) to capture the window even when
        partially obscured. Returns JPEG frames on demand.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_IsCapturing(self, *, metadata: MetadataDict) -> bool:
        """
        Whether the capture thread is currently running.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Whether the capture thread is currently running.
        """

    @abstractmethod
    def StartCapture(self, WindowTitle: str, *, metadata: MetadataDict) -> StartCapture_Responses:
        """

            Starts capturing a window whose title contains the given substring.
            A background thread continuously captures frames.



        :param WindowTitle: Substring to match in the target window's title bar.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def StopCapture(self, *, metadata: MetadataDict) -> StopCapture_Responses:
        """
        Stops the background capture thread.


        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def GetLatestFrame(self, *, metadata: MetadataDict) -> GetLatestFrame_Responses:
        """

            Returns the most recently captured frame as JPEG bytes.



        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - Frame: JPEG image bytes of the latest captured frame.


        """

    @abstractmethod
    def ListWindows(self, *, metadata: MetadataDict) -> ListWindows_Responses:
        """

            Returns a list of visible window titles on the server machine.
            Use these titles with StartCapture to begin streaming.



        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - WindowTitles: List of visible window titles.


        """
