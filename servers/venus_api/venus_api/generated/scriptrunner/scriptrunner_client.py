# Generated by sila2.code_generator; sila2.__version__: 0.14.0
# -----
# This class does not do anything useful at runtime. Its only purpose is to provide type annotations.
# Since sphinx does not support .pyi files (yet?), this is a .py file.
# -----

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:

    from typing import Iterable, Optional

    from scriptrunner_types import (
        DeleteScript_Responses,
        ListScripts_Responses,
        LoadScript_Responses,
        RunScript_IntermediateResponses,
        RunScript_Responses,
        SaveScript_Responses,
        StopScript_Responses,
    )
    from sila2.client import (
        ClientMetadataInstance,
        ClientObservableCommandInstanceWithIntermediateResponses,
        ClientUnobservableProperty,
    )


class ScriptRunnerClient:
    """

    Runs Python automation scripts on the server. Scripts get a pre-built
    `device` helper for calling commands and reading properties on this server.
    Only one script can run at a time. Auto-acquires LockController lock during execution.

    """

    IsRunning: ClientUnobservableProperty[bool]
    """
    Whether a script is currently executing.
    """

    def StopScript(self, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None) -> StopScript_Responses:
        """
        Kill the currently running script.
        """
        ...

    def ListScripts(self, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None) -> ListScripts_Responses:
        """
        List all saved script names.
        """
        ...

    def SaveScript(
        self, ScriptName: str, ScriptCode: str, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> SaveScript_Responses:
        """
        Save a script to the server's script storage.
        """
        ...

    def LoadScript(
        self, ScriptName: str, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> LoadScript_Responses:
        """
        Load a saved script by name.
        """
        ...

    def DeleteScript(
        self, ScriptName: str, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> DeleteScript_Responses:
        """
        Delete a saved script by name.
        """
        ...

    def RunScript(
        self, ScriptName: str, ScriptCode: str, *, metadata: Optional[Iterable[ClientMetadataInstance]] = None
    ) -> ClientObservableCommandInstanceWithIntermediateResponses[RunScript_IntermediateResponses, RunScript_Responses]:
        """

        Execute a Python script. Output is streamed as intermediate responses.
        The script gets a `device` helper for calling commands on this server.

        """
        ...
