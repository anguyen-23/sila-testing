[project]
name = "plate_reader"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "sila2",
    "typer",
    "pywin32",
    "opencv-python",
    "numpy",
    "Pillow",
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["plate_reader*"]

[tool.setuptools.package-data]
"plate_reader.generated" = [
    "*/*.sila.xml",
    "*/*.proto",
]
