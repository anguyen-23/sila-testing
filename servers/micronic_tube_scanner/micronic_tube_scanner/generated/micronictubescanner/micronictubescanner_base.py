# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from datetime import timedelta
from typing import TYPE_CHECKING, List, Optional

from sila2.server import FeatureImplementationBase, MetadataDict, ObservableCommandInstanceWithIntermediateResponses

from .micronictubescanner_types import (
    Connect_Responses,
    Disconnect_Responses,
    GetScanResult_Responses,
    GetScanResultCsv_Responses,
    RescanTube_Responses,
    RetryDecode_Responses,
    ScanBox_IntermediateResponses,
    ScanBox_Responses,
    ScanTube_Responses,
    SetLayout_Responses,
)

if TYPE_CHECKING:

    from ...server import Server


class MicronicTubeScannerBase(FeatureImplementationBase, ABC):
    parent_server: Server

    ScanBox_default_lifetime_of_execution: Optional[timedelta]

    def __init__(self, parent_server: Server):
        """

        Controls a Micronic 2D tube-rack barcode scanner via its REST API.
        Supports rack scanning, single tube scanning, layout management,
        and decode retry/rescan operations. This is a learning feature
        wrapping the Micronic HTTP API (port 2500).

        """
        super().__init__(parent_server=parent_server)

        self.ScanBox_default_lifetime_of_execution = None

    @abstractmethod
    def get_IsConnected(self, *, metadata: MetadataDict) -> bool:
        """
        Whether the scanner API connection is established.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Whether the scanner API connection is established.
        """

    @abstractmethod
    def get_State(self, *, metadata: MetadataDict) -> str:
        """
        Current state of the scanner: idle, scanning, or dataready.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Current state of the scanner: idle, scanning, or dataready.
        """

    @abstractmethod
    def get_CurrentLayout(self, *, metadata: MetadataDict) -> str:
        """
        The currently active rack layout name.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: The currently active rack layout name.
        """

    @abstractmethod
    def get_AvailableLayouts(self, *, metadata: MetadataDict) -> List[str]:
        """
        List of rack layout names supported by the scanner software.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: List of rack layout names supported by the scanner software.
        """

    @abstractmethod
    def get_RackId(self, *, metadata: MetadataDict) -> str:
        """
        The rack ID read from the current rack.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: The rack ID read from the current rack.
        """

    @abstractmethod
    def get_DisplayIcon(self, *, metadata: MetadataDict) -> bytes:
        """
        PNG icon representing this instrument, for display in client UIs.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: PNG icon representing this instrument, for display in client UIs.
        """

    @abstractmethod
    def get_DeviceDescription(self, *, metadata: MetadataDict) -> str:
        """
        Rich text description of device capabilities for LLM system prompts.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Rich text description of device capabilities for LLM system prompts.
        """

    @abstractmethod
    def Connect(self, BaseUrl: str, *, metadata: MetadataDict) -> Connect_Responses:
        """

            Sets the base URL of the Micronic scanner REST API and verifies
            connectivity by querying the device state.



        :param BaseUrl: Base URL of the Micronic API (e.g. "http://10.11.63.32:2500").

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Disconnect(self, *, metadata: MetadataDict) -> Disconnect_Responses:
        """
        Clears the connection to the Micronic scanner.


        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def SetLayout(self, LayoutName: str, *, metadata: MetadataDict) -> SetLayout_Responses:
        """

            Changes the active rack layout used by the scanner software.



        :param LayoutName: Name of the layout to activate (must be from AvailableLayouts).

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def ScanTube(self, *, metadata: MetadataDict) -> ScanTube_Responses:
        """

            Initiates a single tube scan and decode operation.



        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ScanResult: JSON scan result for the single tube.


        """

    @abstractmethod
    def GetScanResult(self, *, metadata: MetadataDict) -> GetScanResult_Responses:
        """

            Returns the scan results of the most recent scan in JSON format.



        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ScanResult: JSON scan result data.


        """

    @abstractmethod
    def GetScanResultCsv(self, *, metadata: MetadataDict) -> GetScanResultCsv_Responses:
        """

            Returns the scan results of the most recent scan in CSV format.



        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - CsvData: Scan result data in CSV format.


        """

    @abstractmethod
    def RetryDecode(
        self, Position: str, Brightness: int, Contrast: int, Threshold: int, *, metadata: MetadataDict
    ) -> RetryDecode_Responses:
        """

            Retries decoding a specified tube with new image parameters
            without rescanning.



        :param Position: Tube position in the rack.

        :param Brightness: Brightness value for decoding.

        :param Contrast: Contrast value for decoding.

        :param Threshold: Threshold value for decoding.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def RescanTube(
        self, Position: str, Brightness: int, Contrast: int, Threshold: int, *, metadata: MetadataDict
    ) -> RescanTube_Responses:
        """

            Rescans and decodes a specified tube with new image parameters.



        :param Position: Tube position in the rack.

        :param Brightness: Brightness value for decoding.

        :param Contrast: Contrast value for decoding.

        :param Threshold: Threshold value for decoding.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def ScanBox(
        self,
        *,
        metadata: MetadataDict,
        instance: ObservableCommandInstanceWithIntermediateResponses[ScanBox_IntermediateResponses],
    ) -> ScanBox_Responses:
        """

            Initiates a full rack scan and decode operation. Polls the device
            state until scanning completes and results are available.



        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        :return:

            - ScanResult: JSON scan result data for the entire rack.


        """
