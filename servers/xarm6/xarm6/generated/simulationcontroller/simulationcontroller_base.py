# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from queue import Queue
from typing import TYPE_CHECKING, Optional, Union

from sila2.server import FeatureImplementationBase, MetadataDict

from .simulationcontroller_types import StartSimulationMode_Responses, StopSimulationMode_Responses

if TYPE_CHECKING:

    from ...server import Server


class SimulationControllerBase(FeatureImplementationBase, ABC):
    parent_server: Server

    _SimulationMode_producer_queue: Queue[Union[bool, Exception]]
    _SimulationMode_current_value: bool

    def __init__(self, parent_server: Server):
        """

        Allows a SiLA Client to control the simulation mode of a SiLA Server.
        In simulation mode, the server simulates device behavior without requiring
        actual hardware. This is useful for testing, development, and learning.

        """
        super().__init__(parent_server=parent_server)

        self._SimulationMode_producer_queue = Queue()

    def update_SimulationMode(self, SimulationMode: bool, queue: Optional[Queue[bool]] = None) -> None:
        """

            Indicates whether the SiLA Server is currently in simulation mode.
            True means the server is simulating, false means real hardware mode.


        This method updates the observable property 'SimulationMode'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._SimulationMode_producer_queue
            self._SimulationMode_current_value = SimulationMode
        queue.put(SimulationMode)

    def SimulationMode_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[bool]]:
        """

            Indicates whether the SiLA Server is currently in simulation mode.
            True means the server is simulating, false means real hardware mode.


        This method is called when a client subscribes to the observable property 'SimulationMode'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_SimulationMode_subscriptions(self, error: Exception, queue: Optional[Queue[bool]] = None) -> None:
        """

            Indicates whether the SiLA Server is currently in simulation mode.
            True means the server is simulating, false means real hardware mode.


        This method aborts subscriptions to the observable property 'SimulationMode'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._SimulationMode_producer_queue
        queue.put(error)

    @property
    def current_SimulationMode(self) -> bool:
        try:
            return self._SimulationMode_current_value
        except AttributeError:
            raise AttributeError("Observable property SimulationMode has never been set")

    @abstractmethod
    def StartSimulationMode(self, *, metadata: MetadataDict) -> StartSimulationMode_Responses:
        """

            Switches the SiLA Server to simulation mode. All subsequent commands
            will be simulated rather than executed on real hardware.



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def StopSimulationMode(self, *, metadata: MetadataDict) -> StopSimulationMode_Responses:
        """

            Switches the SiLA Server out of simulation mode back to real mode.
            All subsequent commands will be executed on real hardware.



        :param metadata: The SiLA Client Metadata attached to the call

        """
