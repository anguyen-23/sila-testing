# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .barcodescanner_types import Connect_Responses, Disconnect_Responses, Scan_Responses, StopScanning_Responses

if TYPE_CHECKING:

    from ...server import Server


class BarcodeScannerBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """

        Controls a Datalogic barcode scanner via RS-232 serial connection.
        Supports triggering scans (STX), stopping scans (ETX), and reading
        barcode data. This is a learning feature wrapping the scanner's
        serial protocol.

        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_IsConnected(self, *, metadata: MetadataDict) -> bool:
        """
        Whether the scanner serial port is currently open.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Whether the scanner serial port is currently open.
        """

    @abstractmethod
    def get_DisplayIcon(self, *, metadata: MetadataDict) -> bytes:
        """
        PNG icon representing this instrument, for display in client UIs.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: PNG icon representing this instrument, for display in client UIs.
        """

    @abstractmethod
    def get_DeviceDescription(self, *, metadata: MetadataDict) -> str:
        """
        Rich text description of device capabilities for LLM system prompts.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Rich text description of device capabilities for LLM system prompts.
        """

    @abstractmethod
    def Connect(self, SerialPort: str, BaudRate: int, *, metadata: MetadataDict) -> Connect_Responses:
        """

            Opens a serial connection to the barcode scanner.



        :param SerialPort: Serial port name (e.g. "COM3" on Windows, "/dev/ttyUSB0" on Linux).

        :param BaudRate: Serial baud rate (default 115200).

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Disconnect(self, *, metadata: MetadataDict) -> Disconnect_Responses:
        """

            Closes the serial connection to the barcode scanner.



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Scan(self, *, metadata: MetadataDict) -> Scan_Responses:
        """

            Triggers the barcode scanner by sending STX and waits for a barcode
            response. Returns the scanned barcode data.



        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - BarcodeData: The scanned barcode string.


        """

    @abstractmethod
    def StopScanning(self, *, metadata: MetadataDict) -> StopScanning_Responses:
        """

            Sends ETX to cancel an active scan operation.



        :param metadata: The SiLA Client Metadata attached to the call

        """
