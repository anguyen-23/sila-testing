# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .camerastreamprovider_types import GetLatestFrame_Responses, StartStream_Responses, StopStream_Responses

if TYPE_CHECKING:

    from ...server import Server


class CameraStreamProviderBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """
        Provides a live camera feed from an IP camera (HTTP/RTSP) or USB webcam.
        The stream can be started with a source URL or device index and frames
        are captured in a background thread. Clients retrieve individual JPEG
        frames via GetLatestFrame, or the web UI can display a continuous
        MJPEG stream via the Flask proxy endpoint.
        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_IsStreaming(self, *, metadata: MetadataDict) -> bool:
        """
        Whether the camera capture is currently active.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Whether the camera capture is currently active.
        """

    @abstractmethod
    def StartStream(self, Source: str, *, metadata: MetadataDict) -> StartStream_Responses:
        """
        Begin capturing frames from a camera source. The Source parameter
            accepts an HTTP/RTSP URL for IP cameras (e.g. "http://192.168.1.100/mjpg/video.mjpg")
            or a device index string for USB webcams (e.g. "0" for the first camera).


        :param Source: Camera source: an HTTP/RTSP URL for IP cameras, or a device
                index (e.g. "0", "1") for USB webcams.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def StopStream(self, *, metadata: MetadataDict) -> StopStream_Responses:
        """
        Stop the camera capture and release the device.


        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def GetLatestFrame(self, *, metadata: MetadataDict) -> GetLatestFrame_Responses:
        """
        Return the most recently captured JPEG frame from the camera.
            The stream must be started first via StartStream.


        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - Frame: The latest camera frame as JPEG binary data.


        """
