# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from datetime import timedelta
from queue import Queue
from typing import TYPE_CHECKING, Optional, Union

from sila2.server import (
    FeatureImplementationBase,
    MetadataDict,
    ObservableCommandInstance,
    ObservableCommandInstanceWithIntermediateResponses,
)

from .multidropcombi_types import (
    Abort_Responses,
    AcknowledgeError_Responses,
    Connect_Responses,
    Disconnect_Responses,
    Dispense_IntermediateResponses,
    Dispense_Responses,
    Empty_Responses,
    MoveOut_Responses,
    Prime_Responses,
    Restart_Responses,
    SetCassetteType_Responses,
    SetColumnVolume_Responses,
    SetDispensingHeight_Responses,
    SetPlateType_Responses,
    SetPumpSpeed_Responses,
    Shake_Responses,
)

if TYPE_CHECKING:

    from ...server import Server


class MultidropCombiBase(FeatureImplementationBase, ABC):
    parent_server: Server

    _IsConnected_producer_queue: Queue[Union[bool, Exception]]
    _IsConnected_current_value: bool

    _InstrumentName_producer_queue: Queue[Union[str, Exception]]
    _InstrumentName_current_value: str

    _FirmwareVersion_producer_queue: Queue[Union[str, Exception]]
    _FirmwareVersion_current_value: str

    _IsPrimed_producer_queue: Queue[Union[bool, Exception]]
    _IsPrimed_current_value: bool

    _DeviceStatus_producer_queue: Queue[Union[str, Exception]]
    _DeviceStatus_current_value: str

    Dispense_default_lifetime_of_execution: Optional[timedelta]

    Shake_default_lifetime_of_execution: Optional[timedelta]

    def __init__(self, parent_server: Server):
        """

        Controls a Thermo Scientific Multidrop Combi reagent dispenser via its
        RS232 serial remote control protocol. Uses Observable properties
        for real-time state push to clients (React WebUI via SSE).

        """
        super().__init__(parent_server=parent_server)

        self._IsConnected_producer_queue = Queue()

        self._InstrumentName_producer_queue = Queue()

        self._FirmwareVersion_producer_queue = Queue()

        self._IsPrimed_producer_queue = Queue()

        self._DeviceStatus_producer_queue = Queue()

        self.Dispense_default_lifetime_of_execution = None
        self.Shake_default_lifetime_of_execution = None

    @abstractmethod
    def get_DisplayIcon(self, *, metadata: MetadataDict) -> bytes:
        """
        PNG icon representing this instrument, for display in client UIs.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: PNG icon representing this instrument, for display in client UIs.
        """

    @abstractmethod
    def get_DeviceDescription(self, *, metadata: MetadataDict) -> str:
        """
        Rich text description of device capabilities for LLM system prompts.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Rich text description of device capabilities for LLM system prompts.
        """

    def update_IsConnected(self, IsConnected: bool, queue: Optional[Queue[bool]] = None) -> None:
        """
        Whether the instrument is currently in remote control mode. Observable for real-time UI updates.

        This method updates the observable property 'IsConnected'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._IsConnected_producer_queue
            self._IsConnected_current_value = IsConnected
        queue.put(IsConnected)

    def IsConnected_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[bool]]:
        """
        Whether the instrument is currently in remote control mode. Observable for real-time UI updates.

        This method is called when a client subscribes to the observable property 'IsConnected'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_IsConnected_subscriptions(self, error: Exception, queue: Optional[Queue[bool]] = None) -> None:
        """
        Whether the instrument is currently in remote control mode. Observable for real-time UI updates.

        This method aborts subscriptions to the observable property 'IsConnected'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._IsConnected_producer_queue
        queue.put(error)

    @property
    def current_IsConnected(self) -> bool:
        try:
            return self._IsConnected_current_value
        except AttributeError:
            raise AttributeError("Observable property IsConnected has never been set")

    def update_InstrumentName(self, InstrumentName: str, queue: Optional[Queue[str]] = None) -> None:
        """
        The instrument name from the VER response, or empty if not connected.

        This method updates the observable property 'InstrumentName'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._InstrumentName_producer_queue
            self._InstrumentName_current_value = InstrumentName
        queue.put(InstrumentName)

    def InstrumentName_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[str]]:
        """
        The instrument name from the VER response, or empty if not connected.

        This method is called when a client subscribes to the observable property 'InstrumentName'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_InstrumentName_subscriptions(self, error: Exception, queue: Optional[Queue[str]] = None) -> None:
        """
        The instrument name from the VER response, or empty if not connected.

        This method aborts subscriptions to the observable property 'InstrumentName'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._InstrumentName_producer_queue
        queue.put(error)

    @property
    def current_InstrumentName(self) -> str:
        try:
            return self._InstrumentName_current_value
        except AttributeError:
            raise AttributeError("Observable property InstrumentName has never been set")

    def update_FirmwareVersion(self, FirmwareVersion: str, queue: Optional[Queue[str]] = None) -> None:
        """
        The firmware version from the VER response, or empty if not connected.

        This method updates the observable property 'FirmwareVersion'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._FirmwareVersion_producer_queue
            self._FirmwareVersion_current_value = FirmwareVersion
        queue.put(FirmwareVersion)

    def FirmwareVersion_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[str]]:
        """
        The firmware version from the VER response, or empty if not connected.

        This method is called when a client subscribes to the observable property 'FirmwareVersion'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_FirmwareVersion_subscriptions(self, error: Exception, queue: Optional[Queue[str]] = None) -> None:
        """
        The firmware version from the VER response, or empty if not connected.

        This method aborts subscriptions to the observable property 'FirmwareVersion'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._FirmwareVersion_producer_queue
        queue.put(error)

    @property
    def current_FirmwareVersion(self) -> str:
        try:
            return self._FirmwareVersion_current_value
        except AttributeError:
            raise AttributeError("Observable property FirmwareVersion has never been set")

    def update_IsPrimed(self, IsPrimed: bool, queue: Optional[Queue[bool]] = None) -> None:
        """
        Whether the pump has been primed since the last connection.

        This method updates the observable property 'IsPrimed'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._IsPrimed_producer_queue
            self._IsPrimed_current_value = IsPrimed
        queue.put(IsPrimed)

    def IsPrimed_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[bool]]:
        """
        Whether the pump has been primed since the last connection.

        This method is called when a client subscribes to the observable property 'IsPrimed'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_IsPrimed_subscriptions(self, error: Exception, queue: Optional[Queue[bool]] = None) -> None:
        """
        Whether the pump has been primed since the last connection.

        This method aborts subscriptions to the observable property 'IsPrimed'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._IsPrimed_producer_queue
        queue.put(error)

    @property
    def current_IsPrimed(self) -> bool:
        try:
            return self._IsPrimed_current_value
        except AttributeError:
            raise AttributeError("Observable property IsPrimed has never been set")

    def update_DeviceStatus(self, DeviceStatus: str, queue: Optional[Queue[str]] = None) -> None:
        """
        Current device status as a JSON string with all state fields (for SSE streaming to WebUI).

        This method updates the observable property 'DeviceStatus'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._DeviceStatus_producer_queue
            self._DeviceStatus_current_value = DeviceStatus
        queue.put(DeviceStatus)

    def DeviceStatus_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[str]]:
        """
        Current device status as a JSON string with all state fields (for SSE streaming to WebUI).

        This method is called when a client subscribes to the observable property 'DeviceStatus'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_DeviceStatus_subscriptions(self, error: Exception, queue: Optional[Queue[str]] = None) -> None:
        """
        Current device status as a JSON string with all state fields (for SSE streaming to WebUI).

        This method aborts subscriptions to the observable property 'DeviceStatus'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._DeviceStatus_producer_queue
        queue.put(error)

    @property
    def current_DeviceStatus(self) -> str:
        try:
            return self._DeviceStatus_current_value
        except AttributeError:
            raise AttributeError("Observable property DeviceStatus has never been set")

    @abstractmethod
    def Connect(self, SerialPort: str, *, metadata: MetadataDict) -> Connect_Responses:
        """

            Opens a serial connection to the Multidrop Combi and sends the VER
            command to enter remote control mode. Returns instrument identification.



        :param SerialPort: Serial port name (e.g. "COM3" on Windows, "/dev/ttyUSB0" on Linux).

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - InstrumentName: The instrument name returned by VER.

            - FirmwareVersion: The firmware version returned by VER.

            - SerialNumber: The instrument serial number returned by VER.


        """

    @abstractmethod
    def Disconnect(self, *, metadata: MetadataDict) -> Disconnect_Responses:
        """

            Sends QIT to exit remote control mode and closes the serial connection.



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Abort(self, *, metadata: MetadataDict) -> Abort_Responses:
        """

            Sends an ESC character to abort the currently running operation.



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Prime(self, Volume: int, Mode: int, *, metadata: MetadataDict) -> Prime_Responses:
        """

            Primes the dispenser hoses by dispensing liquid through them (PRI command).



        :param Volume: Volume in 1/10 uL units.

        :param Mode: Prime mode: 0=all tubes, 1=even tubes, 2=odd tubes, 3=by cassette.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Empty(self, Volume: int, Mode: int, *, metadata: MetadataDict) -> Empty_Responses:
        """

            Empties the dispenser hoses by reversing the pump (EMP command).



        :param Volume: Volume in 1/10 uL units.

        :param Mode: Empty mode: 0=all tubes, 1=by cassette.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def MoveOut(self, *, metadata: MetadataDict) -> MoveOut_Responses:
        """

            Moves the plate carrier to the out/loading position (POU command).



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def AcknowledgeError(self, *, metadata: MetadataDict) -> AcknowledgeError_Responses:
        """

            Acknowledges and clears an instrument error (EAK command).



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def SetPlateType(self, PlateType: int, *, metadata: MetadataDict) -> SetPlateType_Responses:
        """

            Sets the plate type (SPL command). See instrument manual for plate type codes.



        :param PlateType: Plate type code (0-29).

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def SetCassetteType(self, CassetteType: int, *, metadata: MetadataDict) -> SetCassetteType_Responses:
        """

            Sets the dispensing cassette type (SCT command).



        :param CassetteType: Cassette type code (0-3).

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def SetColumnVolume(self, Column: int, Volume: int, *, metadata: MetadataDict) -> SetColumnVolume_Responses:
        """

            Sets the dispensing volume for a specific column (SCV command).



        :param Column: Column number (0=all columns, 1-48=specific column).

        :param Volume: Volume in 1/10 uL units (0 = do not dispense this column).

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def SetDispensingHeight(self, Height: int, *, metadata: MetadataDict) -> SetDispensingHeight_Responses:
        """

            Sets the dispensing height above the plate (SDH command).



        :param Height: Dispensing height in 1/100 mm units.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def SetPumpSpeed(self, Speed: int, *, metadata: MetadataDict) -> SetPumpSpeed_Responses:
        """

            Sets the pump speed as a percentage of maximum (SPS command).



        :param Speed: Pump speed in percent (1-100).

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Restart(self, *, metadata: MetadataDict) -> Restart_Responses:
        """

            Restarts the instrument (RST command). The serial connection will
            be closed and must be re-established with Connect.



        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def Dispense(
        self,
        *,
        metadata: MetadataDict,
        instance: ObservableCommandInstanceWithIntermediateResponses[Dispense_IntermediateResponses],
    ) -> Dispense_Responses:
        """

            Dispenses liquid into the plate (DIS command). Reports column/row
            progress as intermediate responses during dispensing.



        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        :return:

            - Status: Final status message after dispensing completes.


        """

    @abstractmethod
    def Shake(
        self, Time: int, Distance: int, Speed: int, *, metadata: MetadataDict, instance: ObservableCommandInstance
    ) -> Shake_Responses:
        """

            Shakes the plate for a specified duration (SHA command).



        :param Time: Shake duration in 1/100 seconds.

        :param Distance: Shake distance in mm (1-5).

        :param Speed: Shake speed in Hz (1-20).

        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        """
