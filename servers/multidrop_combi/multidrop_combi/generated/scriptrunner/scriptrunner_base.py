# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from datetime import timedelta
from typing import TYPE_CHECKING, Optional

from sila2.server import FeatureImplementationBase, MetadataDict, ObservableCommandInstanceWithIntermediateResponses

from .scriptrunner_types import (
    DeleteScript_Responses,
    ListScripts_Responses,
    LoadScript_Responses,
    RunScript_IntermediateResponses,
    RunScript_Responses,
    SaveScript_Responses,
    StopScript_Responses,
)

if TYPE_CHECKING:

    from ...server import Server


class ScriptRunnerBase(FeatureImplementationBase, ABC):
    parent_server: Server

    RunScript_default_lifetime_of_execution: Optional[timedelta]

    def __init__(self, parent_server: Server):
        """

        Runs Python automation scripts on the server. Scripts get a pre-built
        `device` helper for calling commands and reading properties on this server.
        Only one script can run at a time. Auto-acquires LockController lock during execution.

        """
        super().__init__(parent_server=parent_server)

        self.RunScript_default_lifetime_of_execution = None

    @abstractmethod
    def get_IsRunning(self, *, metadata: MetadataDict) -> bool:
        """
        Whether a script is currently executing.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Whether a script is currently executing.
        """

    @abstractmethod
    def StopScript(self, *, metadata: MetadataDict) -> StopScript_Responses:
        """
        Kill the currently running script.


        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def ListScripts(self, *, metadata: MetadataDict) -> ListScripts_Responses:
        """
        List all saved script names.


        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - Scripts: List of saved script names.


        """

    @abstractmethod
    def SaveScript(self, ScriptName: str, ScriptCode: str, *, metadata: MetadataDict) -> SaveScript_Responses:
        """
        Save a script to the server's script storage.


        :param ScriptName: Name for the saved script.

        :param ScriptCode: Python source code to save.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def LoadScript(self, ScriptName: str, *, metadata: MetadataDict) -> LoadScript_Responses:
        """
        Load a saved script by name.


        :param ScriptName: Name of the script to load.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ScriptCode: The saved Python source code.


        """

    @abstractmethod
    def DeleteScript(self, ScriptName: str, *, metadata: MetadataDict) -> DeleteScript_Responses:
        """
        Delete a saved script by name.


        :param ScriptName: Name of the script to delete.

        :param metadata: The SiLA Client Metadata attached to the call

        """

    @abstractmethod
    def RunScript(
        self,
        ScriptName: str,
        ScriptCode: str,
        *,
        metadata: MetadataDict,
        instance: ObservableCommandInstanceWithIntermediateResponses[RunScript_IntermediateResponses],
    ) -> RunScript_Responses:
        """

            Execute a Python script. Output is streamed as intermediate responses.
            The script gets a `device` helper for calling commands on this server.



        :param ScriptName: A name for this script execution (for logging).

        :param ScriptCode: Python source code to execute.

        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        :return:

            - ExitCode: Process exit code (0 = success).

            - FullOutput: Complete script output as a single string.


        """
